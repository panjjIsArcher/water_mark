import React from 'react'
import ReactDOM from 'react-dom/client'
import  "./assets/util.scss"
import WaterMark from "./utils/utils.ts"

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
  
  </React.StrictMode>
)

export {
	WaterMark
}